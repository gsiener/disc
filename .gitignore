node_modules/
dist/
shots/
.DS_Store
*.log

# agent scratch probes. Node runs TypeScript natively now, so probes are as
# often _foo.ts as _foo.mjs — match the prefix, not the extension.
tools/_*

# local source snapshots
.snapshots/
restored/

# agent scratch
tools/.scratch/

# SwiftPM build products. Goldens under swift/Goldens are generated but ARE
# committed — the diff on a fixture is the record of a deliberate behaviour change.
swift/.build/
*.xcuserdatad
